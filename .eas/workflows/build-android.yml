name: Build Android (Preview)

on:
  push:
    branches: ["main"]

jobs:
  build_android_preview:
    name: Build Android Preview
    type: build
    params:
      platform: android
      profile: preview
